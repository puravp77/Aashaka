git add .
git commit -m "Update content"
git push
npm run deploy


Merged Command:
npx json-server public/data/db.json --port 5000

BACKEND Command
npm run seed:products
npm run dev


17-03-2026 --- BACKEND IMPLEMENTATION


29. Updated admin product create/edit logic in [AdminAddProduct.jsx](/d:/Project/aashaka/src/pages/admin/pages/AdminAddProduct.jsx) to use backend-compatible payloads:
- `name`
- `description`
- `price`
- `category`
- `images`
- `sizes: [{ size, quantity }]`
- `colors`

30. Added shared backend admin product helpers in [adminApi.js](/d:/Project/aashaka/src/utils/adminApi.js) for fetch, create, update, and delete actions.

31. Switched admin product edit/delete operations to use Mongo `_id` instead of legacy `productId` where required by the backend CRUD flow.

32. Extended the admin product table to display real backend fields such as image and size/quantity information while keeping the same table-based UI.

33. Analyzed the admin customers page and found it still depended on mock `/users` and `/orders` data.

34. Added new backend user-management controller logic in [userController.js](/d:/Project/aashaka/backend/controllers/userController.js) to support customer and admin listing via the existing `User` model.

35. Added new backend user routes in [userRoutes.js](/d:/Project/aashaka/backend/routes/userRoutes.js) and registered them in [server.js](/d:/Project/aashaka/backend/server.js).

36. Implemented `GET /api/users` to return real non-admin customers with order counts aggregated from the orders collection.

37. Replaced mock customer loading in [AdminCustomers.jsx](/d:/Project/aashaka/src/pages/admin/pages/AdminCustomers.jsx) with real backend `GET /api/users` and added proper loading and empty states.

38. Analyzed the Allow List page and found it still used separate fake `allowlist` and `users` endpoints from the old json-server setup.

39. Rebuilt [AdminAllowlist.jsx](/d:/Project/aashaka/src/pages/admin/pages/AdminAllowlist.jsx) so it now manages admin users through the real `users` collection using `role: "admin"`.

40. Implemented backend admin-user APIs:
- `GET /api/users/admins`
- `POST /api/users/admins`
- `DELETE /api/users/admins/:id`

41. Ensured `POST /api/users/admins` creates admin users with hashed passwords and returns a proper duplicate-email error when email already exists.

42. Updated [adminApi.js](/d:/Project/aashaka/src/utils/adminApi.js) again to support admin user fetch/create/delete APIs for Customers and Allow List screens.

43. Improved the admin dashboard visuals in [AdminDashboard.jsx](/d:/Project/aashaka/src/pages/admin/pages/AdminDashboard.jsx) by adding metric icons and trend indicators while preserving the same structure.

44. Enhanced admin styling in [AdminPages.css](/d:/Project/aashaka/src/pages/admin/pages/AdminPages.css) with better card elevation, spacing, table padding, hover feel, and typography without changing layout structure.

45. Implemented a global admin theme system by creating [ThemeContext.jsx](/d:/Project/aashaka/src/context/ThemeContext.jsx).

46. Added persistent dark/light theme storage in localStorage via the new theme context, with dark mode as the default.

47. Wrapped the admin app in [AdminApp.jsx](/d:/Project/aashaka/src/AdminApp.jsx) with the global `ThemeProvider`.

48. Refactored [AdminLayout.jsx](/d:/Project/aashaka/src/pages/admin/AdminLayout.jsx) so the existing header theme toggle now uses the global theme context instead of local component state.

49. Preserved the existing `.adm-page.dark-mode` styling system so all existing admin pages continue responding to the theme consistently:
- Dashboard
- Products
- Orders
- Customers
- Allow List

50. Repeatedly verified the project after each major change using backend syntax checks and frontend `npm run build`, and all final changes completed with successful build verification.