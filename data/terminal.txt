git add .
git commit -m "Update content"
git push
npm run deploy


npx json-server public/data/users.json --port 5000





25-02-2026


1. Diagnosed and fixed checkout state and city API issue by replacing deprecated paid endpoint with GitHub-hosted India dataset in PlaceOrder.jsx.

2. Converted Admin Content page into a functional module by implementing controlled form state, Save and Reset actions, and localStorage persistence.

3. Performed multiple npm run build verifications after major changes to ensure final compilation success.



2-3-2026

1. Removed the three top-right header icon buttons (notifications, activity, messages).  
2. Cleaned related icon imports after removing those buttons.  
3. Fixed mobile CSS index-based hide rules affected by header button removal.  
4. Synced `Update` dropdown with `Status` badge in Admin Orders.  
5. Converted row status selector to controlled state handling.  
6. Upgraded row status dropdown UI to a custom styled menu.  
7. Fixed dropdown arrow visibility issue in dark mode.  
8. Applied the same custom dropdown UI to the top Orders filter (`All/Pending/...`).  
9. Applied the same custom dropdown UI to Products category filter (`All/Kurti/...`).  
10. Improved `Day / Month / Year` range tabs styling in dashboard.  
11. Enhanced breadcrumb (`Home / ...`) UI styling for light and dark themes.  
12. Refreshed sidebar visual design (background depth, active indicator, nav hover states).  
13. Added subtle admin animations (sidebar reveal, active glow, card/tab motion).  
14. Fixed animation-related sidebar visibility bug (active item disappearing).  
15. Removed/Restored header quick links segment based on your revert requests.  
16. Removed `Segment` column from Admin Customers table and related logic.  
17. Fixed mobile layout width/overflow issues for admin dashboard/widgets.  
18. Removed `FALLBACK_STATE_NAMES` from Edit Address page and adjusted fallback behavior.  
19. Switched Admin Products to use `public/data/products.json` instead of `allProducts.jsx`.  
20. Added admin allowlist access control (username + email), plus new Admin Allowlist page and routed `Users` quick-link to it.



2-3-2026

1. Updated necklace products `n6` to `n20` with new random names and prices in `public/data/products.json` and `src/data/allProducts.jsx`.  
2. Fixed a temporary JSON syntax break caused during first replacement attempt and restored valid structure.  
3. Re-verified `n6` to `n20` data consistency across both files after repair.  
4. Updated `alsoBought` for `n6` to `n20` so each product has different recommendations.  
5. Removed scroll-reveal animation logic from `Bangles.jsx`, `Oxidised.jsx`, `Necklace.jsx`, and `Earrings.jsx`.  
6. Confirmed `Kurti.jsx` already had no scroll-reveal animation, so no edit was needed there.  
7. Located and shared admin login credentials from project data for testing.  
8. Updated oxidised products `o8` to `o12` (name, price, alsoBought) in `products.json` and `allProducts.jsx`.  
9. Verified `o8` to `o12` values are synced across both files.  
10. Updated bangles `b7` to `b15` with distinct names and prices in `products.json`, `allProducts.jsx`, and `Bangles.jsx`.  
11. Validated `b7` to `b15` consistency across all three files.  
12. Updated earrings `e9` to `e12` with different names and prices in `products.json` and `allProducts.jsx`.  
13. Added a floating WhatsApp button on Home page linked to `+91 92651 69947` (`wa.me` link).  
14. Reduced WhatsApp icon size and added a separate scroll-to-top floating button below it; made arrow appear only after scrolling down.  
15. Improved mobile header/menu UI and fixed hamburger issues by correcting mobile menu interaction/positioning (`pointer-events`, fixed panel placement, compact spacing/typography polish).



2-3-2026 (after 2:30pm)

1. Fixed mobile UI for admin dashboard (`/admin/dashboard`): topbar, breadcrumb, quick actions, card spacing.  
2. Applied responsive fixes across all admin pages for consistency (Dashboard, Products, Orders, Customers, Content, Allow List, Profile, Maintenance Room).  
3. Removed inline layout styles and moved them into reusable CSS classes for cleaner responsive behavior.  
4. Improved Maintenance Room layout (`/admin/maintenance-room`): toggle alignment/size, card balance, shipping form spacing, action button placement.  
5. Fixed dark mode mismatches in maintenance/shipping cards (text, icon tile, alerts, field-related colors).  
6. Fixed dark mode mismatch in dashboard traffic chart (chart panel, grid lines, legend contrast).  
7. Rebuilt the project after each major change (`npm run build`) and confirmed successful compilation.