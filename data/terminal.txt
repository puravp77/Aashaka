git add .
git commit -m "Update content"
git push
npm run deploy


Merged Command:
npx json-server public/data/db.json --port 5000


9-03-2026

**Daily Report**

1. Resolved the admin add/edit product page runtime crash caused by `subCategoryPrefixMap` being referenced before initialization in [AdminAddProduct.jsx](d:/Project/aashaka/src/pages/admin/pages/AdminAddProduct.jsx).

2. Implemented size-based stock validation on the product detail page so users cannot increase quantity beyond the stock available for the selected size in [ProductDetails.jsx](d:/Project/aashaka/src/pages/shop/ProductDetails.jsx).

3. Added `Out of stock for this size` feedback on the product detail page when users try to exceed the available quantity.

4. Updated the product detail page so unavailable sizes stay disabled and selected-size stock information is shown more clearly.

5. Fixed the React hook order issue on the product detail page by ensuring hooks run before any early return conditions in [ProductDetails.jsx](d:/Project/aashaka/src/pages/shop/ProductDetails.jsx).

6. Removed stock labels from inside size buttons because they were breaking the UI layout and kept the stock messaging in a cleaner helper area.

7. Updated Kurti inventory data so products no longer share the same size stock pattern across all items.

8. Applied the revised Kurti stock data to [products.json](d:/Project/aashaka/public/data/products.json) so each product now has distinct size availability.

9. Removed the unused `finalSubCategory` variable from [AdminAddProduct.jsx](d:/Project/aashaka/src/pages/admin/pages/AdminAddProduct.jsx) to clear the lint warning.

10. Updated the admin dashboard metric-card colors to better match the website’s brand styling in [AdminPages.css](d:/Project/aashaka/src/pages/admin/pages/AdminPages.css).

11. Fixed the admin products page edit-button navigation so it uses the real backend product id instead of the uppercase display id in [AdminProducts.jsx](d:/Project/aashaka/src/pages/admin/pages/AdminProducts.jsx).

12. Corrected cart-page quantity behavior so users cannot increase cart quantity beyond the stock available for the selected size in [Cart.jsx](d:/Project/aashaka/src/pages/order/Cart.jsx).

13. Strengthened cart-state handling in [CartContext.jsx](d:/Project/aashaka/src/context/CartContext.jsx) so add-to-cart operations cannot push quantity beyond available stock.

14. Applied the same stock-aware quantity restriction logic to the cart drawer in [CartDrawer.jsx](d:/Project/aashaka/src/components/layout/CartDrawer.jsx).

15. Locked the quantity controls on the product detail page until a size is selected for size-based products in [ProductDetails.jsx](d:/Project/aashaka/src/pages/shop/ProductDetails.jsx).

16. Fixed the admin edit-product pricing calculation so updating the percentage field correctly recalculates the final price in [AdminAddProduct.jsx](d:/Project/aashaka/src/pages/admin/pages/AdminAddProduct.jsx).

17. Added logic to derive the initial percentage on the edit-product page from saved `oldPrice` and `price`, making the edit form more accurate.

18. Fixed the alignment mismatch between the `Actions` table header and the edit/delete buttons on the admin products list in [AdminPages.css](d:/Project/aashaka/src/pages/admin/pages/AdminPages.css).

19. Verified consistency across the main storefront and admin flows so stock limits, size selection rules, and product-edit behavior now work together more reliably.

20. Addressed multiple warnings and regressions during the session, including runtime errors, lint warnings, hook-order issues, id-routing mismatches, stock-validation gaps, and UI alignment issues.


CONTINUE

1. Fixed the unused `allowListPath` warning in [merge_db.jsx](d:/Project/aashaka/merge_db.jsx).

2. Added a CLOTH-specific color-variant structure in the admin add/edit product form in [AdminAddProduct.jsx](d:/Project/aashaka/src/pages/admin/pages/AdminAddProduct.jsx).

3. Updated the admin product payload so CLOTH products can save `variants` with color, images, and size/stock combinations.

4. Preserved compatibility by still filling legacy top-level product fields from the first color variant when saving CLOTH products.

5. Added supporting styles for the new CLOTH color-variant cards and controls in [AdminPages.css](d:/Project/aashaka/src/pages/admin/pages/AdminPages.css).

6. Fixed the initial layout of the new CLOTH color-variant section so the color field, image upload, gallery upload, and size/stock rows render in a cleaner grid.

7. Added dark-mode styling for the new color-variant card so it no longer showed a white/light block inside the dark admin theme.

8. Added dark-mode styling for the `Add Color` and `Remove` buttons so they match the rest of the admin panel in dark mode.

9. Moved the `Color Variants` section to appear after `Product Content` on the add-product page, as requested.

10. Improved spacing and textarea presentation in the `Product Content` section by adding a dedicated section class and adjusting padding, gaps, and textarea height.

11. Styled the `Add Color` and `Remove` buttons with distinct visual colors rather than leaving them as plain neutral buttons.

12. Investigated duplicate image thumbnails on the product page and confirmed the issue came from the saved data containing the same image twice.

13. Fixed the storefront product image loader in [assetPath.js](d:/Project/aashaka/src/utils/assetPath.js) to de-duplicate image arrays before rendering.

14. Fixed the admin save logic in [AdminAddProduct.jsx](d:/Project/aashaka/src/pages/admin/pages/AdminAddProduct.jsx) so duplicate image paths are removed before storing product data.

15. Ensured existing products with duplicate saved image entries no longer show repeated thumbnails on the product details page after reload.