git add .
git commit -m "Update content"
git push
npm run deploy


Merged Command:
npx json-server public/data/db.json --port 5000


9-03-2026

**Daily Report**

1 - 16 SUBMITTED



17. Added logic to derive the initial percentage on the edit-product page from saved `oldPrice` and `price`, making the edit form more accurate.

18. Fixed the alignment mismatch between the `Actions` table header and the edit/delete buttons on the admin products list in [AdminPages.css](d:/Project/aashaka/src/pages/admin/pages/AdminPages.css).

19. Verified consistency across the main storefront and admin flows so stock limits, size selection rules, and product-edit behavior now work together more reliably.

20. Addressed multiple warnings and regressions during the session, including runtime errors, lint warnings, hook-order issues, id-routing mismatches, stock-validation gaps, and UI alignment issues.


CONTINUE

1. Fixed the unused `allowListPath` warning in [merge_db.jsx](d:/Project/aashaka/merge_db.jsx).

2. Added a CLOTH-specific color-variant structure in the admin add/edit product form in [AdminAddProduct.jsx](d:/Project/aashaka/src/pages/admin/pages/AdminAddProduct.jsx).

3. Updated the admin product payload so CLOTH products can save `variants` with color, images, and size/stock combinations.

4. Preserved compatibility by still filling legacy top-level product fields from the first color variant when saving CLOTH products.

5. Added supporting styles for the new CLOTH color-variant cards and controls in [AdminPages.css](d:/Project/aashaka/src/pages/admin/pages/AdminPages.css).

6. Fixed the initial layout of the new CLOTH color-variant section so the color field, image upload, gallery upload, and size/stock rows render in a cleaner grid.

7. Added dark-mode styling for the new color-variant card so it no longer showed a white/light block inside the dark admin theme.

8. Added dark-mode styling for the `Add Color` and `Remove` buttons so they match the rest of the admin panel in dark mode.

9. Moved the `Color Variants` section to appear after `Product Content` on the add-product page, as requested.

10. Improved spacing and textarea presentation in the `Product Content` section by adding a dedicated section class and adjusting padding, gaps, and textarea height.

11. Styled the `Add Color` and `Remove` buttons with distinct visual colors rather than leaving them as plain neutral buttons.

12. Investigated duplicate image thumbnails on the product page and confirmed the issue came from the saved data containing the same image twice.

13. Fixed the storefront product image loader in [assetPath.js](d:/Project/aashaka/src/utils/assetPath.js) to de-duplicate image arrays before rendering.

14. Fixed the admin save logic in [AdminAddProduct.jsx](d:/Project/aashaka/src/pages/admin/pages/AdminAddProduct.jsx) so duplicate image paths are removed before storing product data.

15. Ensured existing products with duplicate saved image entries no longer show repeated thumbnails on the product details page after reload.


10-03-2026

1. Investigated the product-card hover behavior on the kurti/category listing pages.
2. Confirmed the listing card needed deterministic primary/secondary image handling instead of relying on implicit behavior.
3. Updated the category product cards to render both primary and secondary images when available.
4. Added hover crossfade behavior so cards switch to the second image only for products that actually have a second image.
5. Fixed the white/blank hover issue for single-image products by restricting the fade swap to cards with a real secondary image.
6. Verified the category-page hover-image changes with a successful production build.
7. Checked the live Aashaka site structure for `Cloth -> Kurti` and reviewed how the live PDP handles color variants.
8. Analyzed the live product-detail behavior and confirmed that kurti PDPs use shared design-level pages with color variants.
9. Verified multiple live kurti detail pages and confirmed that the sampled/available kurti PDPs on Aashaka include a `Colors:` section.
10. Added kurti product grouping logic so related kurti products can behave as one family with color variants on the PDP.
11. Added alias resolution so old/raw product ids still open the correct grouped product detail page with the intended color preselected.
12. Split product sources into two views: raw products for listing/search visibility and grouped products for PDP variant behavior.
13. Preserved separate kurti cards on the listing page while routing them into grouped variant-aware product details.
14. Improved PDP variant handling so images, sizes, stock, add-to-cart, and wishlist logic follow the selected color variant.
15. Adjusted kurti title handling to avoid bad family-title normalization and keep single-color product titles intact.
16. Audited kurti ids `k1` to `k26` for title-color vs `details.colour` mismatches.
17. Changed kurti variant color labeling to prefer the color parsed from the product title, with fallback to `details.colour`.
18. Hid the `Colors:` section on the PDP when a product has only one real color variant.
19. Replaced loose title-based kurti grouping with an explicit family mapping for true color-family products only.
20. Tuned the `shop/all` hero banner using `images/bannerk.png` by adjusting the dedicated hero height/crop so the face and outfit are more visible, and verified it with a successful build.





16-03-2026

1. Reviewed the existing Aashaka repo and confirmed it was a frontend-first React project using `json-server` and local JSON files for users, products, and orders.

2. Inspected the current local data sources such as [public/data/db.json](/d:/Project/aashaka/public/data/db.json), [tmp_products.json](/d:/Project/aashaka/tmp_products.json), and [tmp_orders.json](/d:/Project/aashaka/tmp_orders.json) to understand the existing storefront data shape.

3. Scanned frontend API usage to identify how the current app talks to the mock backend and where future integration work will be needed.

4. Created a dedicated backend workspace under [backend](/d:/Project/aashaka/backend) with the requested MVC folder structure.

5. Added backend package setup in [backend/package.json](/d:/Project/aashaka/backend/package.json) with Express, Mongoose, JWT, bcrypt, dotenv, cors, and nodemon.

6. Implemented the initial Express server in [server.js](/d:/Project/aashaka/backend/server.js) with middleware, route registration, health check, fallback 404 handling, and centralized error handling.

7. Added MongoDB connection logic in [db.js](/d:/Project/aashaka/backend/config/db.js) using Mongoose.

8. Created initial backend models for User, Product, and Order under [backend/models](/d:/Project/aashaka/backend/models).

9. Created initial route files for authentication, products, and orders under [backend/routes](/d:/Project/aashaka/backend/routes).

10. Created controller files for auth, product, and order flows under [backend/controllers](/d:/Project/aashaka/backend/controllers), covering register/login, product CRUD, and order creation and retrieval.

11. Added JWT authentication middleware in [authMiddleware.js](/d:/Project/aashaka/backend/middleware/authMiddleware.js) for protected routes and role-based checks.

12. Installed all backend dependencies successfully with `npm install` inside [backend](/d:/Project/aashaka/backend).

13. Updated the Mongoose models to match your revised schema requirements for User, Product, and Order, including nested addresses in User and product references inside Order.

14. Realigned the controllers to match the new model structures, including bcrypt-based auth handling, product CRUD with the new fields, and order creation with `products[]`, `totalPrice`, and `orderStatus`.

15. Configured MongoDB Atlas integration by updating [backend/.env](/d:/Project/aashaka/backend/.env) with the Atlas `MONGO_URI` and adjusting startup flow so the server connects to MongoDB before listening.

16. Verified that the backend server now starts successfully with Atlas and logs both the MongoDB connection and server port.

17. Audited route registration and confirmed [server.js](/d:/Project/aashaka/backend/server.js) mounts `/api/auth`, `/api/products`, and `/api/orders` correctly with JSON parsing enabled and fallback 404 response handling.

18. Updated the auth route/controller naming to use `getMe` consistently so `/api/auth/me` matches the expected route contract.

19. Adjusted [productRoutes.js](/d:/Project/aashaka/backend/routes/productRoutes.js) so `POST /api/products` is protected with `protect` exactly as requested for your JWT testing flow.

20. Diagnosed the Thunder Client JWT issue end-to-end: first identified the malformed `Authorization` header format, then confirmed the backend correctly reads Bearer tokens, and finally verified that the remaining `"token invalid"` response is due to the token being stale, malformed, or pasted incorrectly rather than a backend route or middleware bug.